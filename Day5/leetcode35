class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        max1 = max(nums)
        for i in range(0,len(nums)):
            if nums[i] >= target:
                if target > nums[i]:
                    return i+1
                return i
            elif max1<target:
                return len(nums)
